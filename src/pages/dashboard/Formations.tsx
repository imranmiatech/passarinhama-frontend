import { FormationsContent } from "@/components/dashboard/formations/FormationsContent/FormationsContent";

export const Formations = () => {
  return (
    <div className="flex min-h-0 min-w-0 flex-1 flex-col">
      <FormationsContent />
    </div>
  );
};
